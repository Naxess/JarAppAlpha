<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.com220.sli.jarappalpha">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomePage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewMemory"
            android:label="@string/title_activity_create_new_memory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateNewJar"
            android:label="@string/title_activity_create_new_jar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".JarDetail"
            android:label="@string/title_activity_jar_detail"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <meta-data
            android:name="com.facebook.sdk.ApplicationID"
            android:value="@string/facebook_app_id"/>
    </application>

</manifest>
